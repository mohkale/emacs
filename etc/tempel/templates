;; -*- mode: lisp-data -*-
fundamental-mode

(box "┌─" (make-string (length str) ?─) "─┐" n
     "│ " (s str)                       " │" n
     "└─" (make-string (length str) ?─) "─┘" n)

(bang "#!/" (p "usr") "/bin/" q)

emacs-lisp-mode

(autoload ";;;###autoload")
